<table class="table table-bordered genre-table">
  <thead>
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= genre.name %> <!--link_to genre.name, #search_path('search[value]': genre.id, 'search[how]': "match")--> 
        </td>
      </tr>
    <% end %>
  </tbody>
</table>